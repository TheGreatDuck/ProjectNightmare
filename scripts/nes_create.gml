///nes_create(w, h)
//Creates an NES Wall and returns its ID (pointer).
//Width and height are measured in tiles, where one tile is 16x16 pixels (1 PN tile).
return ("whatever Typhon sets the DLL to return")
